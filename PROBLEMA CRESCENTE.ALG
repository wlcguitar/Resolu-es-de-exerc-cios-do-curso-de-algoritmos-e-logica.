Algoritmo "Problema Crescente"

Var

   x, y : inteiro

Inicio

   escreval("Digite dois numeros : ")
   leia(x)
   leia(y)
   
   enquanto x <> y faca
     se x < y então
       escreval("crescente")
     senão
        escreval("Decrescente")
     fimse
      escreval("Digite dois numeros : ")
      leia(x)
      leia(y)

   fimenquanto


Fimalgoritmo